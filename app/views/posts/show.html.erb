<h3><%= @post.title %></h3>

<table>
  <tr>
    <th>開始日</th>
    <td><%= @post.start.strftime("%Y/%m/%d") %></td>
  </tr>
  <tr>
    <th>終了日</th>
    <td><%= @post.finish.strftime("%Y/%m/%d") %></td>
  </tr>
  <tr>
    <th>終日</th>
    <td><%= @post.finish_day %></td>
  </tr>
  <tr>
    <th>スケジュールメモ</th>
    <td><%= @post.memo %></td>
  </tr>
</table>

  <ul>
    <li><%= link_to "一覧に戻る", :posts %></li>
  </ul>
</div>